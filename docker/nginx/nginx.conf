# DEPRECATO — sostituito da Caddy.
#
# Il reverse proxy non è più un container nginx: il TLS e l'instradamento sono
# gestiti da una Caddy che gira sull'host, condivisa con le altre app del server.
#
# Vedi:
#   docker/caddy/blog.menuisland.it.caddy   — il blocco del sito
#   scripts/install-server.sh               — installazione completa
#   docs/DEPLOY.md                          — come funziona
#
# Questo file può essere eliminato: è rimasto solo perché il sistema di file
# della sessione ne impedisce la rimozione automatica.
